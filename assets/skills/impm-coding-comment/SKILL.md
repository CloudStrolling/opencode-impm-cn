---
name: impm-coding-comment
description: 根据 git 修改记录确定本次版本更新的代码范围，并为全部更新代码添加清晰的中文注释
---

# impm-coding-comment 技能

## 触发词
代码备注、代码注释、补充注释、中文注释、coding-comment

## 何时使用
阶段4中，版本回归测试通过后、代码审核前，需要为本次版本更新的全部代码补充中文注释时使用。

## 执行角色
本技能由 文档编写（subagent_type=dw）subagent 负责执行，执行时使用 Skill 工具加载本技能。

## 调度说明（PM/上级编排者启动本技能时必须遵守）
1. 启动方式：使用 task 工具启动 subagent，subagent_type 必须为 `dw`；禁止由 PM 或编排者自己代替执行本技能内容。
2. 提示词必传上下文（缺一不可）：项目根目录绝对路径（projectRoot）、项目英文缩写（{项目英文缩写}）、当前版本号（{当前版本号}）、用户输入 $ARGUMENTS 原文（含用户提到的文件路径）、技能名（impm-coding-comment，要求 subagent 先用 Skill 工具加载本技能再执行）。
3. 完成要求：等待 subagent 返回完成结果后，核对产出文件与 version_progress.md 进度记录，全部正确后才能进入下一步。

## 关键变量定义与取值
| 变量 | 说明 | 获取方式 |
| 项目中文名称 | 项目的中文名称 | 通过 impm_project_info 从 docs/project.md 读取 |
| 项目英文名称 | 项目的英文名称 | 通过 impm_project_info 从 docs/project.md 读取 |
| 项目英文缩写 | 项目的英文缩写，用于拼接所有文档路径 | 通过 impm_project_info 从 docs/project.md 读取 |
| 当前版本号 | 当前执行的版本号 | 通过 impm_version action=current 获取，或从版本目录名推断 |

## 执行要求
1. 严格按照执行步骤中的内容和顺序依次执行：不跳过、不乱序、不并行、不合并任何步骤。
2. 只执行本技能规定的操作，不做与任务无关的工作。
3. 所有文档路径必须用 {项目英文缩写} 和 {当前版本号} 拼接，不得臆造文件名。
4. 使用 impm_* 工具获取信息，不得编造工具返回结果。
5. 全程使用简体中文。
6. 每个步骤完成后，核对产出文件是否存在、内容是否正确。

## 执行步骤
### 步骤 1：查看 git 修改记录，确定本次版本更新的代码范围
1. 调用 impm_git（action=log）查看当前分支的所有提交记录。
2. 调用 impm_git（action=status）查看是否有未提交的修改。
3. 综合提交记录与未提交修改，确定本次版本更新的全部代码文件与修改范围，形成代码文件清单，避免遗漏或误判。

### 步骤 2：为本次版本更新的代码添加中文注释
1. 按代码文件清单逐个检查本次修改的代码。
2. 为函数、类、关键逻辑添加清晰的中文注释：函数/类的功能说明、参数含义、返回值含义；关键业务逻辑的意图与约束条件。
3. 注释要求简洁、准确、与代码行为一一对应，不得臆造代码行为、不得添加无意义注释。
4. 只添加注释，不得修改任何代码逻辑、不得删改已有注释。
5. 全部注释完成后，按项目语言运行编译或语法检查，确认注释没有破坏代码。

### 步骤 3：记录进度
1. 调用 impm_progress add（impm-coding-comment，已完成），在 version_progress.md 中记录本技能完成状态。
2. 核对 version_progress.md 中已记录本步骤状态。

## 交付物
- 本次版本更新代码的中文注释（位于代码文件中）
- version_progress.md 进度记录

## 完成后提示
- 如需继续执行下一步骤，请输入 /impm-coding-review
- 如需继续执行本阶段后续所有步骤，请输入 /impm-finish

<!-- SPDX-License-Identifier: Apache-2.0 / Copyright 2026 jenemy8023 <jenemy8023@163.com> -->
